#include <stdio.h>

// int fib(int index, int n) {
//   if (n <= index) {
//     return n;
//   } else {
//     printf("\n%d", index);
//     return (index + fib(index + 1, n));
//   }
// }
int fib(int n, int *memo) {
  if (n <= 1) {
    return n;
  } else {
  }
}

int contains(int memo[], int value) {
  for (int i = 0; i < 5; i++) {
  }
}

int main() { printf("%d", fib(0, 5)); }
